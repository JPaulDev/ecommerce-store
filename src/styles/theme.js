import {
  borderRadius,
  boxShadows,
  breakpoints,
  colors,
  fontSizes,
} from './primitives';

const theme = {
  breakpoints,
  colors,
  fontSizes,
  borderRadius,
  boxShadows,
};

export default theme;
